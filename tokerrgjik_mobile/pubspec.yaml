name: tokerrgjik_mobile
description: Loja Tradicionale Tokerrgjik - Aplikacion Mobil
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  socket_io_client: ^2.0.3+1
  provider: ^6.1.2
  fl_chart: ^0.68.0
  shared_preferences: ^2.2.3
  # google_mobile_ads: ^5.1.0  # REMOVED - Requires valid AdMob App ID
  audioplayers: ^6.0.0
  http: ^1.2.0
  flutter_animate: ^4.5.0
  share_plus: ^7.2.2
  url_launcher: ^6.2.4
  confetti: ^0.7.0
  flutter_colorpicker: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/sounds/
    # Audio files converted to WAV for better Windows/cross-platform compatibility
